# import pytest
# from registration_slot.models import Reservation
#
# import pytest
#
#
# @pytest.mark.django_db
# def test_reservation_model(client, reservation):
#     assert len(Reservation.objects.all()) == 1
#     assert Reservation.objects.get(name="Load") == reservation
